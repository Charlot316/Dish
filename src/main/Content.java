package main;

import utils.Set;

import javax.swing.*;
import java.awt.*;

public class Content extends JPanel {
    public Content(){
        setBounds(0, 0, Main.WIDTH, Main.HEIGHT);
        setVisible(true);
        setLayout(null);
    }

    public void paint(Graphics g) {
//        Font f = new Font("黑体", Font.BOLD, 20);
//        FontMetrics fm = sun.font.FontDesignMetrics.getMetrics(f);
//        Main.canvas.drawCenteredStringByOutline(g,"搜hhh索",20,20,1,f,20+fm.getHeight(),Color.BLACK,Color.BLACK);

    }
}
